name: Hexo Best Practice 2026

on:
  push:
    branches:
      - main # 只有当你推送源码到 main 时触发

# 必须设置权限，否则部署会报 403 错误
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Generate Hexo
        run: |
          npx hexo clean
          npx hexo generate
          # 执行你的 SEO 脚本
          chmod +x ./tools/generate_sitemap.sh
          ./tools/generate_sitemap.sh

      - name: Upload to Pages Service
        uses: actions/upload-pages-artifact@v3
        with:
          path: './public' # 告诉 GitHub：生成的网页在这里

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 # 最终发布
